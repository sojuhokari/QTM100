source("~/code/QTM100/Lab1Homework.R", echo=TRUE)
# Load the data
source("http://www.openintro.org/stat/data/present.R")
# find the variable names of the data
names(present)
# find the dimensions of the data
dim(present)
# get a summary of the data present in the `year` variable of the dataset
summary(present$year)
# create a vector of bool values, where true means more boys than girls
moreBoysThanGirls <- present$boys > present$girls
# sum the vector
sum(moreBoysThanGirls) # returned 63
# find the number of observations in the data set
length(present$year) # returned 63
# Create a new variable in the data that measures the proportion of boys
present$propBoys <- present$boys / (present$boys + present$girls)
# Plot the data, and label the axes
plot(
x = present$year,
y = present$propBoys,
xlab = "Year",
ylab = "Proportion of births that were male"
)
# Plot the data, but this time add a title
plot(
x = present$year,
y = present$propBoys,
xlab = "Year",
ylab = "Proportion of births that were male",
main = "Proportion of boys over time",
)
# create variable `number_of_years`
number_of_years <- 0
# loop through all proportions of boys in the `present$propBoys` vector.
# If the proportion is more than 0.512, add one to variable `number_of_years`
for (prop in present$propBoys) {
if (prop > 0.512) {
number_of_years = number_of_years + 1
}
}
# print variable `number_of_years`
number_of_years
# create a new variable in the data that measures total births
present$totalBirths <- present$boys + present$girls
# create variables for the year with the most births, and the number of births that year
max_births_year <- 0
max_births_number <- 0
# Loop over all numbers between 1 and the number of observations in the dataset, inclusive.
# For each observation in the dataset, if there are more births that year than
# there were in the current `max_births_number`, update the `max_births_year`
# and `max_births_number`
for (i in 1:nrow(present)) {
if (present$totalBirths[i] > max_births_number) {
max_births_year = present$year[i]
max_births_number = present$totalBirths[i]
}
}
# print the year with the most births
max_births_year
# Find the number of observations
summary(adni)
# Import dataset
adni <- read.csv("datasets/ADNI.txt", header=T)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
adni <- read.csv("datasets/ADNI.txt", header=T)
# Find the number of observations
summary(adni)
View(adni)
# Import dataset
adni <- read.table("datasets/ADNI.txt", header = T)
# Find the number of observations
summary(adni)
View(adni)
# Find the number of observations
length(adni)
View(adni)
# Find the number of observations
length(adni$AGE)
?read.table
View(adni)
# Find the number of observations
length(adni$AGE)
# Get info on the variables and types
str(adni)
adni$APOE4F <- factor(adni$APOE4, labels = c("No APOE4 Gene", "Yes APOE4 Gene"))
View(adni)
adni$APOE4F <- factor(adni$APOE4, labels = c("Variant 1", "Variant 2", "Variant 3"))
summary(adni$APOE4F)
# make a box plot!
boxplot(adni$AGE)
# make a box plot!
boxplot(adni$AGE, ylab="Age (years)")
# Get info on the variables and types
str(adni)
?barplot
# Make a contingency table of adni$DX and adni$MMSE
mmse.table <- table(adni$DX, adni$MMSE)
barplot(mmse.table)
# Make a contingency table of adni$DX and adni$MMSE
mmse.table <- table(adni$MMSE, adni$DX)
barplot(mmse.table)
DX
# Make a contingency table of adni$DX and adni$MMSE
mmse.table <- table(adni$DX, adni$MMSE)
barplot(mmse.table)
barplot(mmse.table, legend.text = T)
barplot(mmse.table, legend.text = T, xlab = "MMSE")
# Make a contingency table of adni$DX and adni$adas
mmse.table <- table(adni$DX, adni$adas)
# Plot a barplot of the data in the contingency table
barplot(mmse.table, legend.text = T, xlab = "adas")
mmse.normal <- adni[adni$DX == "normal"]
mmse.normal <- filter(adni, DX == "Normal")
mmse.normal <- filter(adni, adni$DX == "Normal")
library(dplyr)
mmse.normal <- subset(adni, DX == "Normal")
mmse.normal <- adni[adni$DX == "Normal"]
mmse.normal <- subset(adni, DX == "Normal")
mmse.normal
mmse.mci <- subset(adni, DX == "MCI")
mmse.ad <- subset(adni, DX == "AD")
boxplot(mmse.normal ~ mmse.mci ~ mmse.ad)
boxplot(mmse.normal$MMSE ~ mmse.mci$MMSE ~ mmse.ad$MMSE)
boxplot(mmse.normal$MMSE ~ mmse.mci$MMSE)
boxplot(adni$MMSE ~ adni$DX)
boxplot(adni$MMSE ~ adni$DX, xlab="Alzhemer's Diagnosis", ylab="MMSE")
# Make a side-by-side boxplot comparint adas to DX
boxplot(adni$adas ~ adni$DX, xlab="Alzhemer's Diagnosis", ylab="adas")
# Make a side-by-side boxplot comparint MMSE to DX
boxplot(adni$MMSE ~ adni$DX, xlab="Alzhemer's Diagnosis", ylab="MMSE")
# Make a side-by-side boxplot comparint adas to DX
boxplot(adni$adas ~ adni$DX, xlab="Alzhemer's Diagnosis", ylab="adas")
# Make a side-by-side boxplot comparint MMSE to DX
boxplot(adni$MMSE ~ adni$DX, xlab="Alzhemer's Diagnosis", ylab="MMSE")
# Make a side-by-side boxplot comparint adas to DX
boxplot(adni$adas ~ adni$DX, xlab="Alzhemer's Diagnosis", ylab="adas")
# a. divide the WholeBrain volume
adni$WholeBrainSmall = adni$WholeBrain / 100,000
# a. divide the WholeBrain volume
adni$WholeBrainSmall = adni$WholeBrain / 100000
# a. divide the WholeBrain volume
adni$WholeBrainSmall <- adni$WholeBrain / 100000
summary(ADNI$DX)
summary(adni$DX)
table(adni$DX)
# Turn adni$APOE4 into a factor, and assign it to a new variable in the data set: adni$APOE4
# The three options are now "No copies", "One copy", and "Two copies"
adni$APOE4F <- factor(adni$APOE4, labels = c("No copies", "One copy", "Two copies"))
# Get a summary of the adni$APOE4F variable
summary(adni$APOE4F)
source("~/code/QTM100/Lab2Homework.R", echo=TRUE)
# Get the age in each diagnosis group
table(adni$DX, adni$AGE)
# Get the age in each diagnosis group
table(adni$AGE, adni$DX)
# Get the age in each diagnosis group
addmargins(table(adni$AGE, adni$DX))
# Get the age in each diagnosis group
table(summary(adni$AGE), adni$DX)
# Get the age in each diagnosis group
age.ad <- adni[DX == "AD"]
# Get the age in each diagnosis group
age.ad <- subset(adni, DX == "AD")
# Get the age in each diagnosis group
age.ad <- subset(adni, DX == "AD")
sd(age.ad$AGE)
mean(age.ad$AGE)
sd(age.ad$AGE)
# Create a data subset that only includes observations where the diagnosis
age.ad <- subset(adni, DX == "AD")
# Find the mean and standard deviation for "MCI" and "Normal" as well
age.mci <- subset(adni, DX == "MCI")
mean(age.mci$AGE)
sd(age.mci$AGE)
age.ad <- subset(adni, DX == "AD")
# Find the mean of the subset
mean(age.ad$AGE)
# Find the standard deviation of the subset
sd(age.ad$AGE)
# Find the mean and standard deviation for "MCI" and "Normal" as well
age.mci <- subset(adni, DX == "MCI")
mean(age.mci$AGE)
sd(age.mci$AGE)
age.normal <- subset(adni, DX == "Normal")
mean(age.normal$AGE)
sd(age.normal$AGE)
# Create a data subset that only includes observations where the diagnosis is "AD"
adniAD <- subset(adni, DX == "AD")
# Create a data subset that only includes observations where the diagnosis is "MCI"
adniMCI <- subset(adni, DX == "MCI")
# Create a data subset that only includes observations where the diagnosis is "Normal"
adniNormal <- subset(adni, DX == "Normal")
# Find the mean of the age of the "AD" subset
mean(adniAD$AGE)
# Find the standard deviation of the age of the "AD" subset
sd(adniAD$AGE)
mean(adniMCI$AGE)
sd(adniMCI$AGE)
mean(adniNormal$AGE)
sd(adniNormal$AGE)
sd(adni$AGE)
# Find the mean and standard deviation of the age for all diagnoses
mean(adni$AGE)
mean(adni$WholeBrainSmall)
sd(adni$WholeBrainSmall)
mean(adniAD$WholeBrainSmall)
sd(adniAD$WholeBrainSmall)
mean(adniMCI$WholeBrainSmall)
sd(adniMCI$WholeBrainSmall)
mean(adniNormal$WholeBrainSmall)
sd(adniNormal$WholeBrainSmall)
# Make a contingency table for APOE4 and DX to find the numbers and percentages
table(adni$APOE4, adni$DX)
# Make a contingency table for APOE4 and DX to find the numbers and percentages
table(adni$APOE4F, adni$DX)
# Make a contingency table for APOE4 and DX to find the numbers and percentages
APOE4AndDX <- table(adni$APOE4F, adni$DX)
prop.table(APOE4AndDX)
APOE4AndDX <- table(adni$APOE4F, adni$DX)
prop.table(APOE4AndDX, margin = 1)
# Make a contingency table for APOE4 and DX to find the numbers and percentages
APOE4AndDX <- table(adni$APOE4F, adni$DX)
APOE4AndDX
prop.table(APOE4AndDX, margin = 1)
prop.table(APOE4AndDX, margin = 2)
addmargins(APOE4AndDX)
prop.table(APOE4AndDX, margin = 2)
# make a table with row proportions
prop.table(APOE4AndDX, margin = 2)
table(adni$GENDER, adni$DX)
addmargins(APOE4AndGENDER)
prop.table(APOE4AndGENDER, margin = 2)
APOE4AndGENDER <- table(adni$GENDER, adni$DX)
addmargins(APOE4AndGENDER)
prop.table(APOE4AndGENDER, margin = 2)
# make a table with row proportions
addmargins(prop.table(APOE4AndDX, margin = 2))
# make a table with row proportions
prop.table(addmargins(APOE4AndDX, margin = 2)
# make a table with row proportions
prop.table(addmargins(APOE4AndDX, margin = 2)
# make a table with row proportions
prop.table(addmargins(APOE4AndDX, margin = 2))
# Add margins to get the totals
addmargins(APOE4AndDX)
# Add margins to get the totals
addmargins(APOE4AndDX)
# make a table with row proportions
prop.table(APOE4AndDX, margin = 2)
addmargins(APOE4AndGENDER)
prop.table(APOE4AndGENDER, margin = 2)
source("~/code/QTM100/Lab2Homework.R", echo=TRUE)
source("~/code/QTM100/Lab2Homework.R", echo=TRUE)
mean(adni$WholeBrain)
mean(adni$WholeBrain)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
babies <- read.table("datasets/fruitfly.csv", header=T)
View(babies)
# Import dataset
fruitfly <- read.table("datasets/fruitfly.csv", header=T)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
fruitfly <- read.table("datasets/fruitfly.csv", header=T)
View(fruitfly)
# Import dataset
fruitfly <- read.csv("datasets/fruitfly.csv", header=T)
hist(fruitfly$lifespan)
qqnorm(fruitfly$lifespan)
# add a line to see how close the data is to a normal distribution
qqline(fruitfly$lifespan)
hist(fruitfly$lifespan,
xlab = "lifespan (days)"
)
mean(fruitfly$lifespan)
sd(fruitfly$lifespan)
# store these values in objects
lifemean <- mean(fruitfly$lifespan)
lifesd <- sd(fruitfly$lifespan)
# calculate the probablility of living less than 50 days using pnorm
pnorm(q = 50, mean = 57.4, sd = 17.6)
# Calculate the same probability using the actual data
sum(fruitfly$lifespan<=50)/length(fruitfly$lifespan)
# How can we calculate the upper tail?
1-pnorm(1=50, mean=lifemean, sd=lifesd)
# How can we calculate the upper tail?
1-pnorm(1=50, mean=lifemean, sd=lifesd)
# How can we calculate the upper tail?
1-pnorm(q=50, mean=lifemean, sd=lifesd)
# get a precise percentile
qnorm(p=0.90, mean=lifemean, sd = lifesd)
# Using the actual data to get the percentile
quantile(x=fruitfly$lifespan, probs=0.90)
# calculate the probability of 5 out of 8 flies living longer than 50 days
dbinom(x = 5, size = 8, prob = 0.663)
hat a fruitfly lives longer than 50 days
hat a fruitfly lives longer than 50 days
# calculate the probability of 5 out of 8 flies living longer than 50 days
dbinom(x = 5, size = 8, prob = 0.663)
