source("~/code/QTM100/Lab1Homework.R", echo=TRUE)
# Load the data
source("http://www.openintro.org/stat/data/present.R")
# find the variable names of the data
names(present)
# find the dimensions of the data
dim(present)
# get a summary of the data present in the `year` variable of the dataset
summary(present$year)
# create a vector of bool values, where true means more boys than girls
moreBoysThanGirls <- present$boys > present$girls
# sum the vector
sum(moreBoysThanGirls) # returned 63
# find the number of observations in the data set
length(present$year) # returned 63
# Create a new variable in the data that measures the proportion of boys
present$propBoys <- present$boys / (present$boys + present$girls)
# Plot the data, and label the axes
plot(
x = present$year,
y = present$propBoys,
xlab = "Year",
ylab = "Proportion of births that were male"
)
# Plot the data, but this time add a title
plot(
x = present$year,
y = present$propBoys,
xlab = "Year",
ylab = "Proportion of births that were male",
main = "Proportion of boys over time",
)
# create variable `number_of_years`
number_of_years <- 0
# loop through all proportions of boys in the `present$propBoys` vector.
# If the proportion is more than 0.512, add one to variable `number_of_years`
for (prop in present$propBoys) {
if (prop > 0.512) {
number_of_years = number_of_years + 1
}
}
# print variable `number_of_years`
number_of_years
# create a new variable in the data that measures total births
present$totalBirths <- present$boys + present$girls
# create variables for the year with the most births, and the number of births that year
max_births_year <- 0
max_births_number <- 0
# Loop over all numbers between 1 and the number of observations in the dataset, inclusive.
# For each observation in the dataset, if there are more births that year than
# there were in the current `max_births_number`, update the `max_births_year`
# and `max_births_number`
for (i in 1:nrow(present)) {
if (present$totalBirths[i] > max_births_number) {
max_births_year = present$year[i]
max_births_number = present$totalBirths[i]
}
}
# print the year with the most births
max_births_year
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
gardasil <- read.table("datasets/gardasil.txt", header = T)
# find percent of patients who went to an urban/suburban clinic
prop.table(table(gardasil$LocationType))
# Factor gardasil$LocationType in order to ensure we have the right order
gardasil$LocationTypeF <- factor(gardasil$LocationType, levels=c(
"suburban",
"urban"
))
# Run the test
prop.test(table(gardasil$LocationTypeF), p = 0.70, correct = F)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
gardasil <- read.table("datasets/gardasil.txt", header = T)
# Explore
str(gardasil)
summary(gardasil)
# Create frequency table
Age_Completion_Table <- table(gardasil$AgeGroup, gardasil$Completed)
# View table
Age_Completion_Table
# Add sumary margins
addmargins(Age_Completion_Table)
# Create new variable with correct ordering
gardasil$Completed2 <- factor(gardasil$Completed, levels=c("yes", "no"))
# Create new table
Age_Completion_Table2 <- table(gardasil$AgeGroup, gardasil$Completed2)
Age_Completion_Table2
# Add sumary margins
addmargins(Age_Completion_Table)
# X^2 = 5, df = 1
1 - pchisq(5, df=1)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
evals <- csv("datasets/CourseEvals.csv", header = T)
# Create histogram of the cls_perc_eval variable
hist(evals$cls_perc_eval)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
evals <- csv("datasets/CourseEvals.csv", header = T)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
evals <- read.csv("datasets/CourseEvals.csv", header = T)
# Create histogram of the cls_perc_eval variable
hist(evals$cls_perc_eval)
# Summary
summary(evals$cls_perc_eval)
sd(evals$cls_perc_eval)
# Perform one-sample t-test
t.test(evals$cls_perc_eval, mu=80)
# Adding additional arguments: confidence level
t.test(evals$cls_perc_eval, mu=80, conf.level=0.90)
# Adding additional arguments: confidence level
t.test(evals$cls_perc_eval, mu=80, conf.level=0.99999999999999999)
# Adding additional arguments: confidence level
t.test(evals$cls_perc_eval, mu=80, conf.level=0.999999999)
# Adding additional arguments: confidence level
t.test(evals$cls_perc_eval, mu=80, conf.level=0.9999999999)
# Adding additional arguments: confidence level
t.test(evals$cls_perc_eval, mu=80, conf.level=0.99999999999)
# Adding additional arguments: confidence level
t.test(evals$cls_perc_eval, mu=80, conf.level=0.999999999999)
# Adding additional arguments: confidence level
t.test(evals$cls_perc_eval, mu=80, conf.level=0.90)
# Test evaluating the likelihood that evaluation % is less than 80
t.test(evals$cls_perc_eval, mu=80, alternative="less")
# Perform one-sample t-test
t.test(evals$cls_perc_eval, mu=80)
# Test evaluating the likelihood that evaluation % is less than 80
t.test(evals$cls_perc_eval, mu=80, alternative="less")
# Using the t distribution to get the two-tailed p-value for a one-sample two-
# sided t test
# t = -2, df = 50
2*pt(-2, df=50) # multiply by 2 to make it two-tailed
# Use the qt function to identify a t-score for  aspecific confidence interval
# a 95% confidence interval corresponds to lower tail area of 0.025
1t(0.025, df=50)
# Use the qt function to identify a t-score for  aspecific confidence interval
# a 95% confidence interval corresponds to lower tail area of 0.025
qt(0.025, df=50)
# Import dataset
yrbss <- read.csv("datasets/yrbss.csv", header = T)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
yrbss <- read.table("datasets/yrbss.txt", header = T)
# The working directory. Change this variable when running on a different computer.
workingdir <- "~/code/QTM100"
# Set the working directory
setwd(workingdir)
# Import dataset
yrbss <- read.csv("datasets/yrbss2013.csv", header = T)
days_drink <- yrbss$days_drink
# Summary
summary(days_drink)
hist(days_drink)
# Draw a random sample of size 50 from the vector days_drink
# We're creating a sampling distribution from the "population"
samp_dd1 <- sample(x = days_drink, size = 50)
mean(samp_dd1)
# Draw a random sample of size 50 from the vector days_drink
# We're creating a sampling distribution from the "population"
samp_dd1 <- sample(x = days_drink, size = 50)
mean(samp_dd1)
# Draw a random sample of size 50 from the vector days_drink
# We're creating a sampling distribution from the "population"
samp_dd1 <- sample(x = days_drink, size = 50)
mean(samp_dd1)
sample_means50 <- rep(NA, 5000)
for (i in 1:5000) {
samp_d <- sample(days_drinkg, 50)
sample_means50[i] <- mean(samp_d)
}
for (i in 1:5000) {
samp_d <- sample(days_drink, 50)
sample_means50[i] <- mean(samp_d)
}
# View histogram!
hist(sample_means50)
# Do it again for size 10 and size 100!
sample_means10 <- rep(NA, 5000)
sample_means100 <- rep(NA, 5000)
for (i in 1:5000) {
samp_d <- sample(days_drink, 10)
sample_means10[i] <- mean(samp_d)
samp_d <- sample(days_drink, 100)
sample_means100[i] <- mean(samp_d)
}
hist(sample_means10)
hist(sample_means100)
# PLOT TOGETHER
# divide the plot into two rows and 1 column
par(mfrow = c(2,1))
xlimits <- range(sample_means10)
# PLOT TOGETHER
# divide the plot into two rows and 1 column
par(mfrow = c(2,1))
# set the range of the x-axis of the histograms so they are equal
xlimits <- range(sample_means10)
# plot the histograms
hist(sample_means10, breaks=20, xlim=xlimits)
hist(sample_means100, breaks=20, xlim=xlimits)
# PLOT TOGETHER
# divide the plot into three rows and 1 column
par(mfrow = c(3,1))
# set the range of the x-axis of the histograms so they are equal
xlimits <- range(sample_means10)
# plot the histograms
hist(sample_means10, breaks=20, xlim=xlimits)
hist(sample_means50, breaks=20, xlim=xlimits)
hist(sample_means100, breaks=20, xlim=xlimits)
